{
  "schemaVersion": 1,
  "backup": {
    "service": "backup-runner",
    "schedule": "15 */4 * * *",
    "command": "node ../../node_modules/varlock/bin/cli.js run --inject blob --skip-cache -- node dist/index.mjs backup",
    "targetRpoSeconds": 14400,
    "maximumAgeSeconds": 18000,
    "failureNotificationsRequired": true
  },
  "readiness": {
    "service": "agent",
    "path": "/v1/admin/readiness",
    "accessScope": "admin"
  },
  "residualFailureDomains": [
    "private runtime, OpenBao, telemetry, and local backup share one physical host",
    "source data and independent backup copies share one Cloudflare account"
  ]
}
