name: bob-private-runtime

x-observer-config: &observer-config |-
  extensions:
    health_check:
      endpoint: 0.0.0.0:13133

  receivers:
    host_metrics:
      collection_interval: 30s
      root_path: /hostfs
      scrapers:
        cpu:
        disk:
        filesystem:
          exclude_fs_types:
            fs_types:
              [
                autofs,
                binfmt_misc,
                cgroup2,
                configfs,
                debugfs,
                devpts,
                devtmpfs,
                fusectl,
                hugetlbfs,
                mqueue,
                overlay,
                proc,
                pstore,
                securityfs,
                squashfs,
                sysfs,
                tracefs
              ]
            match_type: strict
        load:
          cpu_average: true
        memory:
        network:
        paging:
        processes:
        system:

    docker_stats:
      endpoint: unix:///var/run/docker.sock
      collection_interval: 30s
      timeout: 10s
      container_labels_to_metric_labels:
        com.docker.compose.service: bob_service
        coolify.managed: coolify_managed
      metrics:
        container.memory.fails:
          enabled: true
        container.restarts:
          enabled: true
        container.uptime:
          enabled: true

    http_check:
      collection_interval: 30s
      metrics:
        httpcheck.response.duration:
          enabled: true
        httpcheck.tls.cert_remaining:
          enabled: true
      targets:
        - endpoints:
            - http://agent:8787/health
            - http://coolify:8080/api/health
            - https://bob-agent.tpops.dev/health
            - https://bob-sendblue.tpops.dev/health

    file_stats/bob_backup:
      include: /backups
      collection_interval: 1m

    prometheus/collector:
      config:
        scrape_configs:
          - job_name: bob-coolify-observer
            scrape_interval: 30s
            static_configs:
              - targets: [127.0.0.1:8888]

  processors:
    memory_limiter:
      check_interval: 5s
      limit_mib: 384
      spike_limit_mib: 96
    resource:
      attributes:
        - action: upsert
          key: deployment.environment.name
          value: prod
        - action: upsert
          key: host.name
          value: bob-coolify
        - action: upsert
          key: service.name
          value: bob-coolify-observer
        - action: upsert
          key: service.namespace
          value: bob
    resource/bob_backup:
      attributes:
        - action: upsert
          key: backup.type
          value: bob
    transform/bob_backup:
      error_mode: propagate
      metric_statements:
        - context: datapoint
          statements:
            - set(attributes["backup_type"], "bob")
    batch: {}

  exporters:
    otlp_http/platform:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:?}
      timeout: 5s

  service:
    extensions: [health_check]
    pipelines:
      metrics:
        receivers:
          - host_metrics
          - docker_stats
          - http_check
          - prometheus/collector
        processors: [memory_limiter, resource, batch]
        exporters: [otlp_http/platform]
      metrics/bob_backup:
        receivers: [file_stats/bob_backup]
        processors: [memory_limiter, resource, resource/bob_backup, transform/bob_backup, batch]
        exporters: [otlp_http/platform]
    telemetry:
      logs:
        level: warn
      metrics:
        readers:
          - pull:
              exporter:
                prometheus:
                  host: 127.0.0.1
                  port: 8888

x-runtime-security: &runtime-security
  cap_drop: [ALL]
  read_only: true
  restart: unless-stopped
  security_opt: [no-new-privileges:true]

services:
  agent-secret-init:
    image: ghcr.io/arek-e/bob-agent@${BOB_AGENT_IMAGE_DIGEST:?}
    command:
      - node
      - -e
      - >-
        const fs=require('node:fs');
        const source='/run/secrets/openbao_approle_secret_id';
        const directory='/run/agent-secrets';
        const target='/run/agent-secrets/openbao_approle_secret_id';
        fs.chownSync(directory,1000,1000);
        fs.chmodSync(directory,0o700);
        fs.copyFileSync(source,target);
        fs.chownSync(target,1000,1000);
        fs.chmodSync(target,0o400)
    restart: "no"
    security_opt: [no-new-privileges:true]
    secrets:
      - source: openbao_approle_secret_id
        target: openbao_approle_secret_id
        uid: "1000"
        gid: "1000"
        mode: 0400
    user: "0:0"
    volumes:
      - /run/bob-agent-secrets:/run/agent-secrets

  agent:
    <<: *runtime-security
    depends_on:
      agent-secret-init:
        condition: service_completed_successfully
    image: ghcr.io/arek-e/bob-agent@${BOB_AGENT_IMAGE_DIGEST:?}
    environment:
      ACCESS_TEAM_DOMAIN: ${ACCESS_TEAM_DOMAIN:?}
      ADMIN_ACCESS_AUDIENCE: ${ADMIN_ACCESS_AUDIENCE:?}
      ADMIN_ACCESS_SUBJECT: ${ADMIN_ACCESS_SUBJECT:?}
      BAO_ADDR: ${BAO_ADDR:?}
      BAO_APPROLE_ROLE_ID: ${BAO_APPROLE_ROLE_ID:?}
      BAO_APPROLE_SECRET_ID_PATH: /run/secrets/openbao_approle_secret_id
      BAO_AUTH_METHOD: approle
      BOB_ALLOWED_MODELS: ${BOB_ALLOWED_MODELS:?}
      BOB_MODEL: ${BOB_MODEL:?}
      BOB_PROVIDER: openai-codex
      BOB_RELEASE_SHA: ${BOB_RELEASE_SHA:?}
      CORE_ACCESS_CLIENT_ID: ${CORE_ACCESS_CLIENT_ID:?}
      CORE_ACCESS_CLIENT_SECRET: ${CORE_ACCESS_CLIENT_SECRET:?}
      CORE_URL: ${CORE_URL:?}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:?}
      PORT: "8787"
      RUN_ACCESS_AUDIENCE: ${RUN_ACCESS_AUDIENCE:?}
      RUN_ACCESS_SUBJECT: ${RUN_ACCESS_SUBJECT:?}
    expose: ["8787"]
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - fetch('http://127.0.0.1:8787/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks: [runtime]
    tmpfs:
      - /tmp:mode=1777,uid=1000,gid=1000
    volumes:
      - /run/bob-agent-secrets:/run/secrets:ro
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 1G
        reservations:
          cpus: "0.1"
          memory: 256M

  tunnel:
    <<: *runtime-security
    image: docker.io/cloudflare/cloudflared@${CLOUDFLARED_IMAGE_DIGEST:?}
    command: [tunnel, --no-autoupdate, --protocol, http2, run]
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARED_TUNNEL_TOKEN:?}
    depends_on:
      agent:
        condition: service_healthy
    networks: [runtime]
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M

  backup-runner:
    <<: *runtime-security
    image: ghcr.io/arek-e/bob-data-backup@${BOB_BACKUP_IMAGE_DIGEST:?}
    command: [sh, -c, "trap 'exit 0' TERM INT; while :; do sleep 60 & wait $!; done"]
    environment:
      BACKUP_AGE_RECIPIENT: ${BACKUP_AGE_RECIPIENT:?}
      BACKUP_COPY_ACCESS_KEY_ID: ${BACKUP_COPY_ACCESS_KEY_ID:?}
      BACKUP_COPY_BUCKET: ${BACKUP_COPY_BUCKET:?}
      BACKUP_COPY_ENDPOINT: ${BACKUP_COPY_ENDPOINT:?}
      BACKUP_COPY_PREFIX: ${BACKUP_COPY_PREFIX:-bob}
      BACKUP_COPY_REGION: ${BACKUP_COPY_REGION:?}
      BACKUP_COPY_SECRET_ACCESS_KEY: ${BACKUP_COPY_SECRET_ACCESS_KEY:?}
      BACKUP_MAX_BYTES: "268435456"
      BACKUP_MIGRATIONS_DIRECTORY: ../../apps/core-worker/migrations
      BACKUP_OUTPUT_DIRECTORY: /backups
      BACKUP_RESTORE_BUCKET_PREFIX: bob-restore-drill
      BACKUP_RESTORE_DATABASE_PREFIX: bob-restore-drill
      BACKUP_RETENTION_COUNT: "42"
      CLOUDFLARE_ACCOUNT_ID: ${CLOUDFLARE_ACCOUNT_ID:?}
      CLOUDFLARE_API_TOKEN: ${CLOUDFLARE_BACKUP_API_TOKEN:?}
      CLOUDFLARE_D1_DATABASE_ID: ${CLOUDFLARE_D1_DATABASE_ID:?}
      R2_ACCESS_KEY_ID: ${R2_BACKUP_ACCESS_KEY_ID:?}
      R2_BUCKET: ${R2_BUCKET:?}
      R2_ENDPOINT: ${R2_ENDPOINT:?}
      R2_SECRET_ACCESS_KEY: ${R2_BACKUP_SECRET_ACCESS_KEY:?}
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - const fs=require('node:fs');const p='/backups';const n=fs.readdirSync(p).filter(x=>/^bob-.*\.json\.gz\.age$/.test(x));if(n.length===0)process.exit(1);const newest=Math.max(...n.map(x=>fs.statSync(p+'/'+x).mtimeMs));if(Date.now()-newest>18000000)process.exit(1)
      interval: 5m
      timeout: 5s
      retries: 1
      start_period: 10m
    networks: [runtime]
    tmpfs:
      - /tmp:mode=1777,uid=1000,gid=1000
    volumes:
      - bob-backups:/backups
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512M
        reservations:
          cpus: "0.05"
          memory: 128M

  observer:
    <<: *runtime-security
    image: docker.io/otel/opentelemetry-collector-contrib@${BOB_OBSERVER_IMAGE_DIGEST:?}
    user: "10001:10001"
    command: [--config=env:OTEL_CONFIG]
    environment:
      OTEL_CONFIG: *observer-config
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:?}
    group_add: ["988"]
    networks: [runtime, coolify]
    volumes:
      - /:/hostfs:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - bob-backups:/backups:ro
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.05"
          memory: 128M

networks:
  runtime:
  coolify:
    external: true

volumes:
  bob-backups:

secrets:
  openbao_approle_secret_id:
    environment: BAO_APPROLE_SECRET_ID
