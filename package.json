{
  "name": "bob",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.19.0",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "check": "pnpm format:check && pnpm boundaries && pnpm deps:check && pnpm env:schema && pnpm env:audit && pnpm infra:load && pnpm typecheck && pnpm test && pnpm build && pnpm ui:safety",
    "boundaries": "node scripts/verify-boundaries.mjs",
    "deps:check": "node scripts/verify-dependency-versions.mjs",
    "env:check": "pnpm -r --if-present env:check",
    "env:audit": "node scripts/audit-env-schemas.mjs",
    "env:schema": "node scripts/validate-env-schemas.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run && pnpm --filter @bob/core-worker test:workers",
    "test:watch": "vitest",
    "typecheck": "pnpm -r --if-present typecheck",
    "db:generate": "pnpm --filter @bob/core-worker db:generate",
    "sendblue:reconcile": "pnpm --filter @bob/sendblue-reconcile reconcile",
    "agent:start": "pnpm --filter @bob/agent start",
    "infra:plan": "pnpm --filter @bob/cloudflare-infra plan",
    "infra:load": "pnpm --filter @bob/cloudflare-infra load:check",
    "secrets:scan:staged": "varlock scan --staged --path apps/core-worker --path apps/sendblue-ingress --path apps/sendblue-egress --path apps/agent --path tools/sendblue-reconcile --path tools/pi-smoke --path infra/cloudflare",
    "secrets:scan:trusted": "varlock scan . --path apps/core-worker --path apps/sendblue-ingress --path apps/sendblue-egress --path apps/agent --path tools/sendblue-reconcile --path tools/pi-smoke --path infra/cloudflare",
    "ui:safety": "node scripts/verify-ui-safety.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.21.0",
    "@cloudflare/workers-types": "5.20260810.1",
    "@effect/vitest": "4.0.0-beta.107",
    "@varlock/hashicorp-vault-plugin": "2.1.0",
    "@types/node": "22.18.0",
    "prettier": "3.9.6",
    "tsx": "4.23.12",
    "typescript": "5.9.3",
    "varlock": "1.16.1",
    "vitest": "4.1.10"
  }
}
