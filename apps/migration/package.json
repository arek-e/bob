{
  "name": "@bob/migration",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=node --target=node22 --outfile=dist/index.js && mkdir -p dist/migrations && cp -R ../../packages/db/service/migrations/. dist/migrations",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@bob/db-service": "workspace:*",
    "effect": "4.0.0-beta.107"
  },
  "devDependencies": {
    "@types/node": "22.18.6",
    "esbuild": "0.25.10",
    "typescript": "5.9.3"
  }
}
