# Browser configuration. Every item in this file is public.
# @defaultSensitive=false
# @defaultRequired=true
# @generateTsTypes(path=./src/environment.generated.ts, exposeEnv=local)
# ---

# Bob serves the private UI and API from one Access-protected origin.
# @auditIgnore
# @type=enum(same-origin)
PUBLIC_API_BASE_URL=same-origin
